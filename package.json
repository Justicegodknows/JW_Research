{
    "name": "jw-research",
    "version": "0.1.0",
    "private": true,
    "description": "JW Research RAG system - monorepo with web, crawler, and indexer",
    "scripts": {
        "dev": "cd web && npm run dev",
        "build": "cd web && npm run build",
        "start": "cd web && npm run start",
        "test": "cd web && npm run test"
    },
    "engines": {
        "node": ">=18.0.0"
    },
    "private": true,
    "dependencies": {
        "next": ">=14.0.0"
    }
}